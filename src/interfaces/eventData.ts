export interface EventData {
  admin: string;
  booked: number;
  coordinates: string;
  driver: string;
  event_id: number;
  is_private: string;
  levels: number[];
  max_participants: number;
  min_participants: number;
  price: number;
  spot: string;
  time_end: string;
  time_start: string;
  waiting: number;
}
